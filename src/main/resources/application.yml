server:
  port: 8081
  # servlet:
  #   context-path: /api   # 注释掉这行
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: system
  datasource:
    url: jdbc:mysql://localhost:3306/login_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 040129
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      file-size-threshold: 2KB
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

mybatis:
  type-aliases-package: com.example.system.entity
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

amap:
  api:
    key: 640e6e3041885522ec573521f426bfcd

logging:
  level:
    root: INFO
    com.example.system: DEBUG